import React from 'react';

import { Container, Loader } from './styles';

const Loading = () => {
  return (
    <Container>
      <Loader></Loader>
    </Container>
  );
};

export default Loading;
